name: JS Actions

on:
  workflow_dispatch:
  issues:
    types: [labeled]

jobs:
  action:
    if: ${{ !github.event.repository.is_template }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: ha-ha
        uses: on-ca/skills-write-javascript-actions/.github/actions/joke-action@main
